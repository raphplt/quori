export const authRoutes = ["/auth/login", "/auth/register"];

export const publicRoutes = ["/", "/auth/callback", "/auth/error", "/api/auth"];

export const protectedRoutes = [
  "/dashboard",
  "/repositories",
  "/admin",
  "/api/protected",
];

export const baseRoute = "/";