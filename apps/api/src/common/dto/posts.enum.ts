export enum PostStatus {
  DRAFT = 'draft',
  READY = 'ready',
  SCHEDULED = 'scheduled',
  PUBLISHED = 'published',
  FAILED = 'failed',
}

export enum PostStatusLinkedin {
  PENDING = 'pending',
  PUBLISHED = 'published',
  FAILED = 'failed',
}

export enum PostRate {
  bad = 'bad',
  neutral = 'neutral',
  good = 'good',
}
